- hosts: localhost
  roles:
    - role: "../.."
      vars:
        test_playground_custom:
          build: true

- hosts: all
  tasks:
    - name: does it work?
      debug:
        var: ansible_lsb
