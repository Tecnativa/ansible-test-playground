- name: get ansible test playground configuration
  set_fact:
    test_playground: "{{
      test_playground_defaults|combine(
        test_playground_custom,
        recursive=True,
      )
    }}"

- import_tasks: build.yml
  when: test_playground.build

- import_tasks: deployment.yml
