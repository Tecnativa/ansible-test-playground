sudo: required
language: python
python:
  - "3.6"
addons:
  apt:
    packages:
      - expect-dev
services:
  - docker
install:
  - pip install -r requirements.txt
before_script:
  - ansible-playbook build/build.yml
script:
  - ./tests/lifecycle.sh
